import { createAuthClient } from "better-auth/react";

//  authClient is an adapter/utility for react components
//  allowing them to interact with better-auth.
export const authClient = createAuthClient();
